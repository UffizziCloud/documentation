import { useEffect } from "react"
import { useRouter } from "next/router"

export default function CLIRedirect() {
  const router = useRouter()

  useEffect(() => {
    router.replace("/docs/cli")
  })

  return null
}