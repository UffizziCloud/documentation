import { useEffect } from "react"
import { useRouter } from "next/router"

export default function Usage() {
  const router = useRouter()

  useEffect(() => {
    router.replace("/faster-e2e-tests-for-github-actions")
  })

  return null
}