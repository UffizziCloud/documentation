import { useEffect } from "react"
import { useRouter } from "next/router"

export default function Usage() {
  const router = useRouter()

  useEffect(() => {
    router.replace("/testing/kubernetes-versions")
  })

  return null
}